// Placeholder. Overwritten at container start (docker-entrypoint.sh) and by `npm run prestart`
// from API_BASE_URL. If it still reads empty at runtime the app throws on purpose - no default.
window.__API_BASE__ = "";
