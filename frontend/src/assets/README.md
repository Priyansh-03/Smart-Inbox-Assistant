# Frontend static assets

Served at `/assets/*` (see `angular.json` → `assets`).

## TODO

- [ ] **Edit / refresh `Priyansh_Srivastava_Resume.pdf`** — currently a copy of
  `Priyansh_Srivastava_exp.pdf`. Update the résumé content (roles, dates, this
  project) and drop the new PDF here with the same filename so the header
  "Résumé" link keeps working. Linked from the top bar in
  `src/app/app.component.ts`.
