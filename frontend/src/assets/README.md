# Frontend static assets

Served at `/assets/*` (see `angular.json` → `assets`).

- `clinevo-logo.jpg` — top-bar brand mark.
- `favicon.png` — browser tab icon.
- `Priyansh_Srivastava_Resume.pdf` — downloaded by the header "Résumé" link
  (`src/app/app.component.ts`, `dev.resume` in `src/app/constants.ts`). `*.pdf`
  here is git-ignored.
